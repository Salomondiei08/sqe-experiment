# Objective Completion Audit

This audit maps the active paper objective to current artifacts. It is not a
result source.

## Objective

Produce a rigorous, coherent LaTeX paper package for the SQE experiment, with
real experiment results, tables, figures, author metadata, a GitHub placeholder,
and explicit protection against hallucinated or simulated evidence. Do not
delete server files.

## Prompt-to-Artifact Checklist

| Requirement | Evidence | Status |
|---|---|---|
| Rerun/update the experiment package | `scripts/run_paper_pipeline.sh`; latest generated `paper/main.pdf`; `ARTIFACT_MANIFEST.json`; post-generation verifier command recorded below | Done for retrieval package |
| Real experiment tables and figures | `paper/tables/*.tex`; `paper/figures/*.png`; `paper/table_inventory.json`; generated from executed result JSON/JSONL files and deterministic diagnostics over executed rows/saved indexes; every active table has declared source files in `active_table_sources` | Done for retrieval metrics |
| High-quality LaTeX paper | `paper/main.tex`; `paper/main.pdf`; `LATEX_BUILD_AUDIT.json`; verifier checks PDF text, table inputs, figures, author, GitHub placeholder, and a warning-free Tectonic build | Professional retrieval-study draft done; final top-conference empirical package still blocked by missing Pass@1 and human labels |
| Conference-style layout preview | `scripts/36_make_conference_preview.py`; `paper/main_conference_preview.tex`; `paper/main_conference_preview.pdf`; `CONFERENCE_PREVIEW_AUDIT.json`; explicitly marked as not an official venue template and not experiment evidence | Done as optional layout preview |
| Conference presentation for tomorrow | `presentation/sqe_conference_slides.html`; `presentation/sqe_conference_speaker_notes.md`; `presentation/README.md`; included in `ARTIFACT_MANIFEST.json` and `github_code_release/RELEASE_MANIFEST.json`; uses verified retrieval results only and states missing Pass@1 and human-label evidence | Done as HTML deck |
| Professional table layout | `paper/main.tex`; generated `table` environments with captions, labels, body references, `booktabs`, no vertical rules, and improved float spacing; visual PDF spot checks on rendered pages | Done |
| Float and table spacing guard | `scripts/37_audit_paper_style.py`; `PAPER_STYLE_AUDIT.json`; `scripts/14_submission_readiness_check.py`; `scripts/07_verify_experiment.py`; verifies caption spacing, float spacing, table column spacing, row-height controls, and controlled `\emergencystretch` overflow handling are present in `paper/main.tex` | Done |
| Main method/result figures | `paper/figures/method_overview.png`; `paper/figures/recall_at_5.png`; `paper/figures/gate_diagnostic.png`; `paper/figures/threshold_sensitivity.png`; all are numbered and referenced in `paper/main.tex` | Done |
| Figure readability | Generated figures are 8-bit RGB PNGs with lighter colors, titles, legends or axis labels, and body references; `LATEX_BUILD_AUDIT.json` reports no overfull/underfull boxes or unresolved references | Done |
| Figure asset quality guard | `scripts/39_audit_figure_assets.py`; `FIGURE_ASSET_AUDIT.json`; `scripts/14_submission_readiness_check.py`; `scripts/07_verify_experiment.py`; active figures must have PNG and SVG sources, sufficient size, non-dark luminance, nonblank content, and SVG text elements | Done |
| Non-AI-sounding style guard | `scripts/37_audit_paper_style.py`; `PAPER_STYLE_AUDIT.json`; `scripts/14_submission_readiness_check.py`; `scripts/07_verify_experiment.py`; package-level checks reject em/en dashes, curly quotes, global `\sloppy`, excessive manual bolding, and a guarded list of hype or vague phrases, including `extense`, `extensive`, `extensively`, and `robust` in active paper text; readiness and the main verifier now require those guarded terms to appear in the audit configuration | Done |
| Citation metadata quality | `paper/references.bib`; `scripts/07_verify_experiment.py` check `references_bib`; latest verifier reports no missing required keys, no loose placeholder-style entries, and no missing required venue/DOI/page metadata | Done |
| Handoff status | `HANDOFF_STATUS.md`; `scripts/07_verify_experiment.py` check `handoff_status_missing_required_phrases=[]`; manifest includes the file | Done |
| Related-work framing | `paper/main.tex`; generated by `scripts/06_make_paper_artifacts.py`; verifier requires `Related Work` in compiled `paper/main.pdf` text | Done |
| Threats-to-validity framing | `paper/main.tex`; generated by `scripts/06_make_paper_artifacts.py`; verifier requires `Threats to Validity` in compiled `paper/main.pdf` text | Done |
| Conclusion | `paper/main.tex`; generated by `scripts/06_make_paper_artifacts.py`; verifier requires `Conclusion` in compiled `paper/main.pdf` text | Done |
| Author is Salomon DIEI | `paper/main.tex`; verifier field `paper_author_present=true` | Done |
| GitHub placeholder | `paper/main.tex`; verifier field `github_placeholder_present=true` | Done |
| GitHub code-release helper | `scripts/34_prepare_github_code_release.py`; local `github_code_release/README.md`; local `github_code_release/RELEASE_MANIFEST.json`; verifier field `github_release_script_present=true` | Done locally; not uploaded |
| GitHub code-release safety guard | `scripts/07_verify_experiment.py` checks `github_code_release/RELEASE_MANIFEST.json` when present and fails if it declares or lists raw memory stores, detailed query rows, Pass@1 results, human labels, synthetic replacement rows, or nested release directories | Done |
| Hugging Face dataset-release code | `scripts/33_prepare_hf_dataset_release.py`; local `hf_dataset_release/README.md`; local `hf_dataset_release/release_manifest.json`; paper and table include `TODO: add Hugging Face dataset URL` | Done locally; not uploaded |
| No hallucinated, fake, placeholder, or simulated evidence | `NO_HALLUCINATED_DATA.md`; `CLAIM_AUDIT.md`; `paper/table_inventory.json`; verifier fields `forbidden_synthetic_evidence_references=[]`, `unsupported_claim_references=[]`, `table_inventory_active_mismatch=[]`, `claim_audit_missing_required_phrases=[]`, `checklist_missing_quarantine_phrases=[]`, `checklist_forbidden_placeholder_claims=[]`, and `no_hallucinated_data_missing_required_phrases=[]` | Done |
| Active paper evidence-claim guard | `scripts/43_audit_paper_evidence_claims.py`; `PAPER_EVIDENCE_CLAIM_AUDIT.json`; `SUBMISSION_READINESS.json` check `Paper evidence-claim audit`; verifier check `paper_evidence_claim_audit`; rejects positive Pass@1, task-success, or human-validation claims while the corresponding readiness gates remain missing | Done |
| Random-gated evidence is executed JSONL | Executed rows in `results_500_memory_seed*/random_budget_detailed.jsonl`; `CLAIM_AUDIT.md`; `DATA_PROVENANCE.md` | Done |
| Multi-seed retrieval evidence | `results_500_memory_seed42/`, `results_500_memory_seed43/`, `results_500_memory_seed44/`, `results_500_memory_seed45/`, `results_500_memory_seed46/`, `results_500_memory_seed47/`, `results_500_memory_seed48/`, `results_500_memory_seed49/`; `results_multiseed/multiseed_report.json` | Done for eight independent memory-index seeds |
| Gate-feature diagnostics | `results_gate_calibration/gate_feature_diagnostics.json`; `paper/tables/gate_feature_diagnostics.tex`; verifier field `gate_feature_table_missing_expected_rows=[]` | Done as diagnostic; does not close dense-comparison blocker |
| Held-out gate paired diagnostic | `results_gate_calibration/gate_validation_paired_tests.json`; `paper/tables/gate_validation_paired_tests.tex`; `scripts/28_gate_validation_paired_tests.py`; aggregate delta +1.1 Recall@5 points, CI [+0.1, +2.1] over 2,000 held-out rows | Non-blocking diagnostic; not Pass@1 evidence |
| Cross-seed top-1 gate diagnostic | `results_gate_calibration/cross_seed_top1_gate.json`; `paper/tables/cross_seed_top1_gate.tex`; `scripts/29_cross_seed_top1_gate.py`; `SUBMISSION_READINESS.json`; verifier field `cross_seed_top1_gate_table_missing_expected_rows=[]` | Weak; CI crosses zero |
| Cross-seed score/BM25 gate-variant diagnostic | `results_gate_calibration/cross_seed_gate_variant.json`; `paper/tables/cross_seed_gate_variant.tex`; `scripts/32_cross_seed_gate_variant.py`; `paper/table_inventory.json`; `DATA_PROVENANCE.md` | Retained diagnostic only; not SQE method evidence |
| Strong claim over Dense-Only | `results_multiseed/multiseed_paired_tests.json`; `SUBMISSION_READINESS.json` | Supported only as a small retrieval-metric improvement; not downstream/task-success evidence |
| Downstream Pass@1/task-success evidence | `PASS1_HARNESS_AUDIT.md`; schema-only `PASS1_RESULTS_SCHEMA.md`; expected future `results_pass1/` files; importer `scripts/23_import_evoagentbench_pass1.py`; guarded summary helper `scripts/21_summarize_pass1_results.py`; verifier `scripts/17_verify_pass1_results.py` | Missing |
| Pass@1 harness execution preflight | `pass1_harness_preflight.json`; `scripts/26_check_pass1_harness_readiness.py`; EvoAgentBench SWE-bench parquet/split metadata; Docker socket permission check; 2026-05-15 `docker ps`/`sudo -n docker ps`/`getent group docker` checks | Blocked by OS Docker permission |
| Alternate container runtime diagnostic | `pass1_harness_preflight.json`; `SUBMISSION_READINESS.json`; `scripts/26_check_pass1_harness_readiness.py`; `scripts/14_submission_readiness_check.py`; `scripts/07_verify_experiment.py`; records that `nerdctl` is present but cannot list containers and is not a drop-in replacement for the Docker-SDK-based SWE-bench adapter | Done; diagnostic only, not Pass@1 evidence |
| Future EvoAgentBench run configs | `pass1_evoagentbench_configs/config_dense_codex.yaml`, `config_always_expand_codex.yaml`, `config_random_gated_codex.yaml`, `config_selective_codex.yaml`; matching software-engineering domain configs; `pass1_evoagentbench_configs/RUNBOOK.md`; local EvoAgentBench Codex adapter | Prepared for four-method run; not result evidence |
| Future guarded Pass@1 runner | `scripts/42_run_pass1_after_preflight.sh`; reruns preflight, refuses unless `ready_to_run_pass1=true`, runs four paper methods on the same task set, keeps smoke output in `results_pass1_smoke/`, requires 500 paired task IDs for full `results_pass1/` evidence, imports/summarizes/verifies, and refuses overwrite unless `PASS1_OVERWRITE=1` | Prepared; not result evidence |
| Future external-evidence resume helper | `scripts/44_resume_after_external_evidence.sh`; after real `results_pass1/` rows and real human labels exist, summarizes and verifies both evidence gates, refreshes readiness files, release bundles, the artifact manifest, and the final verifier report; refuses to continue if external evidence is missing | Prepared; not result evidence |
| Reproducible command wrapper | `Makefile`; exposes non-destructive targets for `paper`, `verify`, `readiness`, `release`, `manifest`, and guarded `external-evidence-resume`; documented in `README.md` | Done |
| Human-audited query quality labels | `human_audit/README.md`; `human_audit/verification_report.json`; template-only `human_audit/human_audit_labeling_manifest.template.json`; expected future labeled CSV/manifest/summary; summary helper `scripts/20_summarize_human_audit_labels.py`; verifier `scripts/18_verify_human_audit_labels.py` | Missing |
| Human-audit summary provenance guard | `scripts/20_summarize_human_audit_labels.py`; `scripts/18_verify_human_audit_labels.py`; future `human_audit_summary.json` must declare `artifact_type=human_audit_summary`, `is_experiment_evidence=true`, `source_csv=human_audit_queries.csv`, `labeled_csv=labeled_human_audit_queries.csv`, and `labeling_manifest=human_audit_labeling_manifest.json` | Done as guard; not result evidence |
| Human-audit reviewer/adjudication guard | `scripts/18_verify_human_audit_labels.py`; `scripts/14_submission_readiness_check.py`; `scripts/07_verify_experiment.py`; `human_audit/LABELING_PROTOCOL.md`; `human_audit/human_audit_labeling_manifest.template.json`; future manifest must list at least two reviewers and nonempty `adjudication_notes`, and readiness cannot mark labels valid without those guards plus summary provenance | Done as guard; not result evidence |
| Remaining unblock steps | `BLOCKED_NEXT_ACTIONS.md`; `SUBMISSION_READINESS.json` check `Documented unblock steps`; `scripts/07_verify_experiment.py` readiness cross-check; `pass1_harness_preflight.json`; `human_audit/verification_report.json` | Documented; not evidence |
| Machine-readable missing-evidence blockers | `MISSING_EVIDENCE_BLOCKERS.json`; generated by `scripts/35_write_missing_evidence_blockers.py`; records the two remaining evidence gates, required future files, verification commands, and non-evidence artifacts | Done; documentation only |
| Human-audit labeling helper | `scripts/27_make_human_audit_labeling_form.py`; `human_audit/labeling_form.html`; `human_audit/labeling_form_manifest.json` | Prepared; not result evidence |
| Human-audit reviewer packets | `scripts/40_make_human_audit_reviewer_packets.py`; `scripts/41_verify_human_audit_reviewer_packets.py`; `human_audit/reviewer_packets/`; `human_audit/reviewer_packets_verification.json`; source rows copied with blank label fields only | Prepared and verified; not result evidence |
| Human-audit manifest date guard | `scripts/18_verify_human_audit_labels.py`; temporary verifier checks reject malformed dates such as `2026/05/15` and accept exact `YYYY-MM-DD` format in structurally valid temporary packets | Done; guard only, not result evidence |
| Do not delete server files | No deletion command is part of the pipeline or this audit; `scripts/10_prepare_eval_from_memory.py` now refuses destructive `--overwrite`; release helpers treat `--clean` as a deprecated no-op; `SUBMISSION_READINESS.json` includes `Project script deletion safety`; the main verifier checks release-helper and project-script deletion safety | Maintained |

## External Guideline Alignment

Checked on 2026-05-15 against official public author guidance. This section is
documentation only and is not experiment evidence.

| Source | Relevant check | Current package status |
|---|---|---|
| ACLPUB formatting guidance, `https://acl-org.github.io/ACLPUB/formatting.html` | Main-text figures and tables should be numbered sequentially and have captions | `PAPER_STYLE_AUDIT.json` finds 4 captioned/labeled/referenced figures and 17 captioned/labeled/referenced tables |
| ACM LaTeX best practices, `https://www.acm.org/publications/taps/latex-best-practices` | Use standard LaTeX figure/table interfaces; figure captions after figures and table captions before tables | `paper/main.tex` uses standard figure/table environments, `booktabs` tables, and explicit caption spacing controls |
| NeurIPS paper checklist, `https://nips.cc/public/guides/PaperChecklist` | Experimental papers should document reproducibility, code/data access, and limitations | `paper/main.tex` includes reproducibility, limitations, threats to validity, GitHub/Hugging Face placeholders, and explicit missing-evidence statements |

This check supports formatting and reproducibility presentation only. It does
not close the missing Pass@1/task-success or human-label evidence blockers.

## Latest Verification Snapshot

Checked on 2026-05-15 KST from `/home/nlp-07/sqe_experiment` after the layout,
figure, GitHub/Hugging Face, dataset-release, and documentation timestamp
refreshes.

Current verification commands:

```bash
bash scripts/run_paper_pipeline.sh
/home/nlp-07/evermemos/EverOS/methods/evermemos/.venv/bin/python \
  scripts/14_submission_readiness_check.py --root /home/nlp-07/sqe_experiment \
  --output SUBMISSION_READINESS.json
/home/nlp-07/evermemos/EverOS/methods/evermemos/.venv/bin/python \
  scripts/35_write_missing_evidence_blockers.py --root /home/nlp-07/sqe_experiment \
  --output MISSING_EVIDENCE_BLOCKERS.json
/home/nlp-07/evermemos/EverOS/methods/evermemos/.venv/bin/python \
  scripts/33_prepare_hf_dataset_release.py --root /home/nlp-07/sqe_experiment \
  --output_dir /home/nlp-07/sqe_experiment/hf_dataset_release \
  --include_detailed_results
/home/nlp-07/evermemos/EverOS/methods/evermemos/.venv/bin/python \
  scripts/34_prepare_github_code_release.py --root /home/nlp-07/sqe_experiment \
  --output_dir /home/nlp-07/sqe_experiment/github_code_release \
  --include_result_summaries
/home/nlp-07/evermemos/EverOS/methods/evermemos/.venv/bin/python \
  scripts/13_make_artifact_manifest.py --root /home/nlp-07/sqe_experiment \
  --output ARTIFACT_MANIFEST.json
python3 scripts/07_verify_experiment.py \
  --data_dir data_500_memory_seed42 \
  --index_dir index_500_seed42 \
  --results_dir results_500_memory_seed42 \
  --paper_dir paper \
  --report_path /tmp/sqe_verify_latest.json
```

Observed pipeline and verifier status:

- Latest full pipeline run completed with:
  `Paper package regenerated and verified for independent memory-index seeds 42, 43, 44, 45, 46, 47, 48, and 49.`
- `failures: []`
- `forbidden_synthetic_evidence_references: []`
- `invalid_or_deprecated_artifact_references: []`
- `unsupported_claim_references: []`
- `legacy_random_budget_doc_references: []`
- `claim_audit_missing_required_phrases: []`
- `no_hallucinated_data_missing_required_phrases: []`
- `paper_author_present: true`
- `github_placeholder_present: true`
- `huggingface_placeholder_present: true`
- `hf_release_script_present: true`
- `main_pdf_bytes: 473040`
- `artifact_manifest_files: 242`
- `main_pdf_text_checked: true`
- `PAPER_STYLE_AUDIT.json`: `clean=true`, `n_figures=4`,
  `n_tables=17`, `em_dash_count=0`, `manual_bold_count=0`,
  all figure/table labels are referenced, every active figure has a nonempty
  description, `forbidden_unicode_punctuation_counts={}`, and
  `hype_or_vague_phrase_hits=[]`
- `FIGURE_ASSET_AUDIT.json`: `clean=true`, `n_active_figures=4`;
  all active PNG figures have matching SVG sources, mean luminance above 236,
  dark-pixel share below 1.1%, and no row-level failures.
- `PAPER_EVIDENCE_CLAIM_AUDIT.json`: `clean=true`, `matches=[]`,
  `missing_required_limitations=[]`; positive Pass@1, task-success, and
  human-validation claims are blocked while those evidence gates are missing.
- `LATEX_BUILD_AUDIT.json`: `clean_build=true`, `warnings=[]`,
  `failures=[]`, `pdf_pages=11`, `pdf_bytes=473040`
- `ARTIFACT_MANIFEST.json`: `n_files=242`
- `github_code_release/RELEASE_MANIFEST.json`: `n_files=210`,
  `contains_pass1_results=false`, `contains_human_labels=false`,
  `contains_synthetic_replacement_rows=false`
- `hf_dataset_release/release_manifest.json`: `n_files=168`,
  `strong_submission_ready=false`, `contains_pass1_results=false`,
  `contains_human_labels=false`, `contains_synthetic_replacement_rows=false`
- `readiness_latex_status: pass`
- `readiness_latex_clean_build: true`
- `readiness_latex_warnings: []`
- `readiness_latex_failures: []`
- `readiness_latex_pdf_pages: 11`
- `readiness_paper_style_forbidden_unicode_punctuation_counts: {}`
- `readiness_paper_style_hype_or_vague_phrase_hits: []`
- `readiness_figure_asset_status: pass`
- `readiness_figure_asset_clean: true`
- `readiness_figure_asset_failures: []`
- `readiness_figure_asset_n_active_figures: 4`
- `strong_submission_ready: false`
- `submission_blocking_count: 2`
- `readiness_pass1_status: missing`
- `readiness_pass1_has_results: false`
- `readiness_pass1_preflight_checked_at_utc: 2026-05-15T06:50:02+00:00`
- `readiness_pass1_preflight_ready: false`
- `readiness_pass1_preflight_blockers: ["current user cannot access Docker daemon"]`
- `readiness_pass1_docker_socket_group: docker`
- `readiness_pass1_docker_user_group_names: ["nlp-07"]`
- `readiness_pass1_docker_user_in_socket_group: false`
- `readiness_pass1_docker_user_listed_in_socket_group: false`
- `readiness_pass1_importer_present: true`
- `readiness_pass1_importer_is_evidence: false`
- `readiness_pass1_schema_is_evidence: false`
- `readiness_external_resume_helper_status: pass`
- `readiness_external_resume_helper_valid: true`
- `readiness_external_resume_helper_is_evidence: false`
- `readiness_external_resume_helper_missing_required_snippets: []`
- `readiness_external_resume_helper_forbidden_snippets_present: []`
- `scripts/17_verify_pass1_results.py` now requires Dense-Only,
  Always-Expand, Random-Gated-Expansion, and Selective-QE rows over the same
  task set by default.
- `scripts/21_summarize_pass1_results.py` now uses the same default
  four-method task-set gate before writing `pass1_summary.json`; partial
  downstream runs must remain unsummarized until all required method rows are
  present.
- `Pass@1 retrieval context packets: pass`
- `pass1_contexts/`: verified Dense-Only, Always-Expand,
  Random-Gated-Expansion, and Selective-QE context packets for 500 SWE-bench
  Verified tasks each; context packets are non-result prompt inputs only
- `readiness_human_label_status: missing`
- `readiness_human_label_valid: false`
- `readiness_human_label_labeled_rows: 0`
- `readiness_human_label_complete_rows: 0`
- `readiness_human_source_packet_is_evidence: false`
- `readiness_human_template_is_evidence: false`
- `scripts/run_paper_pipeline.sh` refreshes
  `human_audit/verification_report.json`; the expected missing-label verifier
  failure is non-fatal and is surfaced by `SUBMISSION_READINESS.json`
- `table_inventory_source_key_mismatch: []`
- `table_inventory_empty_source_tables: []`
- `table_inventory_missing_source_paths: []`
- `case_analysis_table_missing_expected_rows: []`
- `cross_seed_top1_gate_table_missing_expected_rows: []`
- `main_tex_table_inputs` includes `gate_validation_paired_tests`
- `gate_validation_paired_tests` has declared source
  `results_gate_calibration/gate_validation_paired_tests.json`
- `main_tex_table_inputs` includes `cross_seed_top1_gate`
- `cross_seed_top1_gate` has declared source
  `results_gate_calibration/cross_seed_top1_gate.json`
- Latest explicit no-skip verifier report:
  `/tmp/sqe_verify_latest.json`, with `warnings=[]`,
  `failures=[]`, and `artifact_manifest_stale_entries=[]`.

Additional `paper/main.pdf` check after the latest run:

- `Pages: 11`
- `File size: 473040 bytes`

Additional `ARTIFACT_MANIFEST.json` check after the latest run:

- `n_files: 242`
- `files: 242`
- Official seed verification reports were refreshed after the latest readiness
  and preflight update:
  `results_500_memory_seed42/verification_report.json` through
  `results_500_memory_seed49/verification_report.json`.
- The final no-skip verifier pass after rebuilding the manifest completed with
  `warnings: []` and `failures: []`.

Additional `LATEX_BUILD_AUDIT.json` check after the latest run:

- `returncode: 0`
- `warnings: []`
- `failures: []`
- `clean_build: true`
- `pdf_bytes: 473040`
- `pdf_pages: 11`

Additional figure/layout checks after the latest run:

- `paper/figures/method_overview.png`: 2520 x 985, 8-bit RGB
- `paper/figures/recall_at_5.png`: 1879 x 1145, 8-bit RGB
- `paper/figures/gate_diagnostic.png`: 1879 x 985, 8-bit RGB
- `paper/figures/threshold_sensitivity.png`: 1879 x 985, 8-bit RGB
- `pdftotext paper/main.pdf -` shows body references and numbered captions
  for Figure 1 through Figure 4 and Table 1 through Table 17.
- The paper source contains no em dash character.

Additional Hugging Face release check after the latest run:

```bash
python3 scripts/33_prepare_hf_dataset_release.py --include_detailed_results
```

Observed output:

```json
{
  "output_dir": "/home/nlp-07/sqe_experiment/hf_dataset_release",
  "status": "prepared"
}
```

This creates a local release directory only. It does not upload to Hugging Face
and does not create human labels, downstream Pass@1 rows, or synthetic
replacement data.

Additional GitHub code-release check after the latest run:

```bash
python3 scripts/34_prepare_github_code_release.py --include_result_summaries
```

Observed output:

```json
{
  "output_dir": "/home/nlp-07/sqe_experiment/github_code_release",
  "n_files": 210
}
```

This creates a local code-release directory only. It does not create a Git
repository, upload to GitHub, package raw memory stores, package detailed
per-query JSONL rows, or create missing human-label or Pass@1 evidence.
The verifier now checks the release manifest and fails if those exclusions are
violated.

The active paper package contains `Random-Gated-Expansion` only as an executed
baseline backed by JSONL result rows. Deprecated random-gated table artifacts
remain quarantined as non-evidence and are blocked from active paper references
by the verifier.
The active paper now refers to the comparison baseline as the executed
random-gating budget control. The verifier rejects the old budget-control label
and synthetic-evidence wording in active paper artifacts.
The verifier also rejects legacy random-budget labels and synthetic-evidence
wording in paper-facing documentation, and `SUBMISSION_READINESS.json` surfaces
`legacy_random_budget_doc_references` for each official seed verification
report.
The verifier now also requires `CLAIM_AUDIT.md` and
`NO_HALLUCINATED_DATA.md` to state that measured-token reruns support cost
claims only, Pass@1 schemas and human-label templates are not evidence, and
active paper tables require declared source provenance.

A deterministic qualitative case table was added to the active paper from real
seed-42 retrieval rows only:

- `paper/tables/case_analysis.tex`
- Sources declared in `paper/table_inventory.json`:
  `data_500_memory_seed42/eval_pairs.jsonl`,
  `results_500_memory_seed42/dense_only_detailed.jsonl`, and
  `results_500_memory_seed42/proposed_detailed.jsonl`
- The table contains two SQE top-5 gains and two SQE top-5 losses, selected
  deterministically by query ID. These rows are illustrative examples, not
  additional aggregate metrics.
- `scripts/07_verify_experiment.py` now recomputes the expected case-table
  rows from those source JSONL files and fails if the active LaTeX table drifts
  from the data.
- Official verification reports were regenerated with the strengthened verifier:
  `results_500_memory_seed42/verification_report.json` through
  `results_500_memory_seed49/verification_report.json`. Each report has
  `failures: []`, `stale_seed_provenance_doc_references: []`, and
  `stale_gate_variant_ci_doc_references: []`. Seed 42 includes
  `case_analysis_table_missing_expected_rows: []`; non-paper-source seed
  reports retain `paper_table_consistency_scope: skipped for non-paper-source
  seed results`.
- Full end-to-end regeneration was rerun with `bash scripts/run_paper_pipeline.sh`.
  It completed with the message:
  `Paper package regenerated and verified for independent memory-index seeds 42, 43, 44, 45, 46, 47, 48, and 49.`
- `scripts/run_paper_pipeline.sh` refreshes tracked official seed verification
  reports once before readiness and again after `SUBMISSION_READINESS.json` is
  current, so the tracked reports embed the final Pass@1 preflight and
  readiness snapshot before `ARTIFACT_MANIFEST.json` is rebuilt.
- `scripts/run_paper_pipeline.sh` now also regenerates
  `MISSING_EVIDENCE_BLOCKERS.json`, `hf_dataset_release/release_manifest.json`,
  and `github_code_release/RELEASE_MANIFEST.json` after readiness is current,
  so release and blocker artifacts are rebuilt from the same state as the paper.
- The verifier now blocks unsupported strength phrases such as `significantly
  improves`, `clear improvement`, `robust improvement`, and `end-to-end
  improvement` in active paper artifacts unless the corresponding evidence is
  added. The current package passes this language guard with
  `unsupported_claim_references: []`.
- Syntax checks passed after verifier and paper-package updates:
  `/home/nlp-07/evermemos/EverOS/methods/evermemos/.venv/bin/python -m
  py_compile scripts/*.py` and `bash -n` over the active shell entry points,
  including `scripts/run_paper_pipeline.sh`.
- `README.md` now lists the active `paper/tables/case_analysis.tex` artifact
  and documents both the deterministic case-table recomputation guard and the
  unsupported strength-language guard.
- The verifier now extracts text from `paper/main.pdf` and fails on unresolved
  LaTeX markers such as `??` or `[?]`. The current PDF passes with
  `main_pdf_unresolved_markers: []`.

Negative evidence checks were also run to ensure documentation-only artifacts do
not accidentally satisfy missing result requirements:

```bash
/home/nlp-07/evermemos/EverOS/methods/evermemos/.venv/bin/python \
  scripts/17_verify_pass1_results.py --results_dir results_pass1
```

This correctly failed because `results_pass1/` does not exist and no
`manifest.json`, `pass1_summary.json`, or `*_detailed.jsonl` task rows exist.
`scripts/23_import_evoagentbench_pass1.py` can import completed EvoAgentBench
job outputs into that schema, but no completed SQE method-comparison job has
been imported into `results_pass1/`.
The Pass@1 importer and readiness scripts compile successfully:

```bash
/home/nlp-07/evermemos/EverOS/methods/evermemos/.venv/bin/python \
  -m py_compile \
  scripts/14_submission_readiness_check.py \
  scripts/23_import_evoagentbench_pass1.py \
  scripts/17_verify_pass1_results.py \
  scripts/21_summarize_pass1_results.py
```

The Pass@1 summary helper now also blocks incomplete paper-method evidence
before writing a summary. The default summary and verifier require the four
paper methods over the same task set with at least 500 paired task IDs; smoke
runs must lower this threshold explicitly and write to `results_pass1_smoke/`.
Focused checks after the guard update:

```bash
python3 -m py_compile scripts/21_summarize_pass1_results.py \
  scripts/17_verify_pass1_results.py
python3 scripts/21_summarize_pass1_results.py \
  --results_dir /tmp/sqe_missing_pass1_results
python3 scripts/21_summarize_pass1_results.py \
  --results_dir /tmp/sqe_partial_pass1_results
python3 scripts/21_summarize_pass1_results.py \
  --results_dir /tmp/sqe_complete_pass1_results \
  --min_task_count 1 \
  --output /tmp/sqe_complete_pass1_results/pass1_summary.json
python3 scripts/17_verify_pass1_results.py \
  --results_dir /tmp/sqe_complete_pass1_results \
  --min_task_count 1 \
  --output /tmp/sqe_complete_pass1_verify.json
```

The missing-results and Selective-QE-only temporary fixtures failed before
summary creation. The complete one-task temporary four-method fixture failed
under the default 500-task paper threshold and summarized/verified only when
`--min_task_count 1` was set explicitly. These temporary fixtures are verifier
tests only and are not experiment evidence.

```bash
/home/nlp-07/evermemos/EverOS/methods/evermemos/.venv/bin/python \
  scripts/18_verify_human_audit_labels.py --audit_dir human_audit
```

This correctly failed because the source audit packet is unlabeled and the
required `labeled_human_audit_queries.csv`,
`human_audit_labeling_manifest.json`, and `human_audit_summary.json` files do
not exist.
The human-audit summarizer was also smoke-tested on temporary data:

```bash
/home/nlp-07/evermemos/EverOS/methods/evermemos/.venv/bin/python \
  scripts/20_summarize_human_audit_labels.py \
  --audit_dir /tmp/sqe_human_summary_test \
  --output /tmp/sqe_human_summary_test/human_audit_summary.json
```

The valid temporary labeled packet produced a 100-row summary and passed the
verifier only when the summary carried the required provenance fields
`artifact_type`, `is_experiment_evidence`, `source_csv`, `labeled_csv`, and
`labeling_manifest`, and when the manifest listed two reviewers plus
`adjudication_notes`. A tampered packet with changed source fields failed before
writing a summary. These temporary labels are not paper evidence.
The readiness check was also smoke-tested on a separate one-row temporary packet
whose labels and summary were complete but whose manifest listed only one
reviewer. `human_audit_status` reported `valid: false`,
`labeling_manifest_reviewer_count: 1`, and
`labeling_manifest_has_two_reviewers: false`. This verifies that filled labels
alone cannot satisfy the human-audit evidence gate.

The full paper pipeline was also rerun after adding table-source provenance:

```bash
bash scripts/run_paper_pipeline.sh
```

It completed successfully with the final message:

```text
Paper package regenerated and verified for independent memory-index seeds 42, 43, 44, 45, 46, 47, 48, and 49.
```

The generated seed verification reports all had `failures: []`, and the table
inventory source checks passed with no missing, empty, or mismatched active
table source entries.

The full paper pipeline was rerun again after adding the cross-seed top-1 gate
diagnostic:

```bash
bash scripts/run_paper_pipeline.sh
```

It completed successfully with the final message:

```text
Paper package regenerated and verified for independent memory-index seeds 42, 43, 44, 45, 46, 47, 48, and 49.
```

The new diagnostic is based only on executed Dense-Only and Always-Expand rows:
train on seven independent memory-index seeds, test on the held-out seed, and
repeat for all eight seeds. It evaluates 4,000 held-out query/seed rows and
reports a +0.625 Recall@5 point difference with 95% CI [-0.075, +1.325], so it
remains diagnostic evidence only, not support for a strong effectiveness claim.
The verifier now requires the generating script
`scripts/29_cross_seed_top1_gate.py`, the JSON report, and the LaTeX table to
all appear in `ARTIFACT_MANIFEST.json`.

After the random-gated baseline wording cleanup, the official seed verification
reports were regenerated and `SUBMISSION_READINESS.json` was refreshed. A final
freshness verifier pass wrote `/tmp/sqe_fresh_after_readiness.json` with:

- `failures: []`
- `artifact_manifest_stale_entries: []`
- `legacy_random_budget_doc_references: []`
- `forbidden_synthetic_evidence_references: []`
- `unsupported_claim_references: []`
- `checklist_missing_quarantine_phrases: []`
- `checklist_forbidden_placeholder_claims: []`
- `no_hallucinated_data_missing_required_phrases: []`

After adding the non-destructive release-helper readiness guard, the official
seed verification reports were refreshed again. Each seed report now records
`readiness_release_helpers_status: pass`, empty
`readiness_release_helpers_destructive_patterns`, and empty
`readiness_release_helpers_missing_noop_clean_guard`.
The project-wide deletion-safety readiness guard also scans non-audit scripts
for command-like file deletion patterns. The current readiness artifact reports
`Project script deletion safety: pass` with `destructive_patterns: []`. The old
`--overwrite` deletion branch in `scripts/10_prepare_eval_from_memory.py` was
replaced with a refusal that asks for `--resume` or a new output directory.

After adding the generator-formatting guard, the official seed verification
reports were refreshed again. Each seed report now records
`paper_generator_professional_formatting` with figure descriptions present and
no forced `[H]` figure-placement patterns.

The active paper-facing wording is now `Random-Gated-Expansion` as an executed
random-gating budget control backed by JSONL rows. The old internal
`random_budget_*` filenames remain only as provenance paths for those executed
rows.

The active paper now includes a short `Related Work` section covering dense and
sparse retrieval, HyDE-style query expansion, Reciprocal Rank Fusion, and
SWE-bench-style task-success evaluation. This section is generated by
`scripts/06_make_paper_artifacts.py`, appears in `paper/main.tex`, and is
present in the compiled PDF. The verifier now requires the phrase `Related Work`
in `paper/main.pdf`; the no-skip verifier pass at
`/tmp/sqe_related_work_guard_final_noskip.json` completed with:

- `failures: []`
- `artifact_manifest_stale_entries: []`
- `main_pdf_missing_phrases: []`
- `main_pdf_unresolved_markers: []`
- `unsupported_claim_references: []`

The active paper also includes a generated `Threats to Validity` section with
construct, data, method, and external-validity threats. After adding it, the PDF
was rebuilt directly with Tectonic and produced no TeX warnings. The final
no-skip verifier pass at `/tmp/sqe_warning_free_threats_final.json` completed
with:

- `failures: []`
- `artifact_manifest_stale_entries: []`
- `main_pdf_missing_phrases: []`
- `main_pdf_unresolved_markers: []`

The active paper now includes a generated `Conclusion` section that restates the
measured retrieval finding without adding new claims: SQE reduces expansion
calls relative to always-on expansion and improves over Hybrid-RRF/trace-only
expansion, but remains essentially tied with Dense-Only and Random-Gated-
Expansion. The verifier now requires `Conclusion` in the compiled PDF. The
warning-free PDF rebuild on 2026-05-15 KST produced `paper/main.pdf` with 11
pages and 473040 bytes, and the latest official verification reports completed
with:

- `failures: []`
- `artifact_manifest_stale_entries: []`
- `main_pdf_missing_phrases: []`
- `main_pdf_unresolved_markers: []`

The clean-build audit is now a first-class verification gate. The script
`scripts/31_verify_latex_clean_build.py` compiles `paper/main.tex` with
Tectonic and writes `LATEX_BUILD_AUDIT.json`. `SUBMISSION_READINESS.json` and
`scripts/07_verify_experiment.py` now require that audit to report:

- `returncode: 0`
- `warnings: []`
- `failures: []`
- `clean_build: true`

The final no-skip verifier pass at
`/tmp/sqe_final_after_persisted_latex_gate.json` completed with:

- `failures: []`
- `artifact_manifest_stale_entries: []`
- `readiness_latex_clean_build: true`
- `readiness_latex_warnings: []`
- `readiness_latex_failures: []`
`SUBMISSION_READINESS.json` now includes a non-blocking
`Cross-seed top-1 gate diagnostic` row with status `weak` and records the JSON
path, table path, script path, Recall@5 values, confidence interval,
sign-flip p-value, and expansion rate.

An additional retained cross-seed score/BM25 gate-variant diagnostic was added:

- Script: `scripts/32_cross_seed_gate_variant.py`
- JSON: `results_gate_calibration/cross_seed_gate_variant.json`
- Table: `paper/tables/cross_seed_gate_variant.tex`
- Provenance: `DATA_PROVENANCE.md`
- Inventory: `paper/table_inventory.json` retained table entry
- Result: 69.5% gate Recall@5 versus 68.5% Dense-Only over 4,000 held-out
  rows, delta +0.975 points, 95% CI [+0.25, +1.70], sign-flip p=0.009,
  expansion rate 44.3%. This is retained as a diagnostic, not as SQE
  effectiveness evidence.

This diagnostic uses only executed Dense-Only, BM25-Only, and Always-Expand
JSONL rows. It does not run query expansion, does not generate LLM data, and
does not support a strong SQE effectiveness claim because it is a separate
score/BM25 gate diagnostic rather than the proposed SQE method or downstream
Pass@1 evidence.

Final no-skip verifier passes after adding the retained diagnostic and the
latest paper-framing patch:

- `/tmp/sqe_verify_seed42_after_gate_variant_fresh.json`: `failures: []`
- `/tmp/sqe_verify_seed47_after_gate_variant_fresh.json`: `failures: []`
- `/tmp/sqe_verify_seed42_after_framing_patch.json`: `failures: []`,
  `artifact_manifest_stale_entries: []`,
  `unsupported_claim_references: []`,
  `legacy_random_budget_doc_references: []`,
  `no_hallucinated_data_missing_required_phrases: []`
- `/tmp/sqe_verify_after_all_seed_refresh_and_manifest.json`: `failures: []`,
  `artifact_manifest_stale_entries: []`,
  `stale_seed_provenance_doc_references: []`,
  `stale_gate_variant_ci_doc_references: []`
- `/tmp/sqe_verify_after_readiness_crosscheck_guard.json`: `failures: []`,
  `readiness_missing_guard_fields: []`,
  `readiness_nonempty_guard_fields: []`,
  `stale_seed_provenance_doc_references: []`,
  `stale_gate_variant_ci_doc_references: []`
- `/tmp/sqe_verify_after_readiness_paper_claim_audit.json`: `failures: []`,
  `readiness_pass1_preflight_checked_at_utc: 2026-05-15T07:41:49+00:00`,
  `readiness_pass1_preflight_ready: false`,
  `readiness_pass1_preflight_blockers: ["current user cannot access Docker daemon"]`,
  `readiness_pass1_docker_socket_group: docker`,
  `readiness_pass1_docker_user_group_names: ["nlp-07"]`,
  `readiness_pass1_docker_user_in_socket_group: false`,
  `readiness_project_deletion_safety_status: pass`,
  `readiness_project_deletion_safety_destructive_patterns: []`,
  `readiness_pass1_docker_user_listed_in_socket_group: false`,
  `readiness_pass1_guarded_runner_smoke_output_separate: true`,
  `readiness_pass1_guarded_runner_full_min_task_count: true`,
  `readiness_pass1_guarded_runner_smoke_min_task_count_explicit: true`
- `/tmp/sqe_verify_after_fresh_preflight.json`: `failures: []`,
  `artifact_manifest_stale_entries: []`,
  `readiness_pass1_preflight_checked_at_utc: 2026-05-15T06:50:02+00:00`,
  `readiness_pass1_preflight_ready: false`,
  `readiness_pass1_preflight_blockers: ["current user cannot access Docker daemon"]`,
  `readiness_pass1_docker_socket_group: docker`,
  `readiness_pass1_docker_user_group_names: ["nlp-07"]`,
  `readiness_pass1_docker_user_in_socket_group: false`,
  `readiness_pass1_docker_user_listed_in_socket_group: false`
- `/tmp/sqe_verify_after_resume_helper_guard.json`: `failures: []`,
  `artifact_manifest_stale_entries: []`,
  `readiness_external_resume_helper_status: pass`,
  `readiness_external_resume_helper_valid: true`,
  `readiness_external_resume_helper_is_evidence: false`,
  `readiness_external_resume_helper_missing_required_snippets: []`,
  `readiness_external_resume_helper_forbidden_snippets_present: []`
- `/tmp/sqe_verify_after_handoff_makefile_note.json`: `failures: []`,
  `artifact_manifest_stale_entries: []`
- `/tmp/sqe_verify_latest.json`: `failures: []`,
  `artifact_manifest_stale_entries: []`
- 2026-05-15 approval-path `sudo usermod -aG docker nlp-07` attempt failed
  with `sudo: a terminal is required to read the password`; no group change was
  made.
- 2026-05-15 `sudo -n true` failed with `sudo: a password is required`.
  No group change was made; Docker remains blocked for this user.
- `ARTIFACT_MANIFEST.json`: `n_files: 242`
- `LATEX_BUILD_AUDIT.json`: `clean_build: true`, `warnings: []`,
  `failures: []`, `pdf_bytes: 473040`

The paper and guardrail docs now distinguish generated hypothetical traces from
factual execution logs. `paper/main.tex` says generated traces are retrieval
probes rather than claims that executions occurred. `NO_HALLUCINATED_DATA.md`,
`DATA_PROVENANCE.md`, and `CLAIM_AUDIT.md` state that `expansion_cache.json`
and generated traces are method inputs, not task-success evidence, human
labels, or standalone empirical evidence. `scripts/06_make_paper_artifacts.py`
now emits the same corrected wording, so regenerating the paper does not
reintroduce the old misleading phrase. The verifier now requires these
guardrail phrases.

The bibliography was also tightened from loose placeholder-style entries to
venue/DOI/page-aware BibTeX entries for HyDE, RRF, SWE-bench, DPR, and BM25.
This was a paper-quality metadata change only; it did not alter experiment
metrics.
The main verifier now checks `paper/references.bib` for required citation keys,
forbidden loose entries such as `journal={Proceedings of ACL}`, and required
venue/DOI/page metadata where available.
`HANDOFF_STATUS.md` was added as documentation-only transfer guidance. The
main verifier now checks that it names the current paper artifacts, both
remaining strong-submission blockers, the Docker admin command, and the
non-placeholder evidence rule.
The main verifier also rejects stale Pass@1 harness wording that would imply
retrieval-context injection is still missing after the optional context hook was
added; the current blocker is Docker access plus missing completed task runs.
The Pass@1 verifier also rejects full paper evidence with fewer than 500 paired
task IDs, so one-task smoke output cannot satisfy the paper evidence gate.

The latest paper-framing patch makes the 8-seed aggregate the primary retrieval
evidence and states that SQE does not establish a practically meaningful
advantage over Dense-Only or a clear advantage over Random-Gated-Expansion.
This is a wording/evidence-alignment change only; no metric files were changed.

## Current Completion Judgment

The retrieval-paper package is regenerated, internally verified, and protected
against hallucinated evidence. The broader objective of a strong top-conference
empirical paper is not complete because two required evidence conditions are
not satisfied:

- The multi-seed Recall@5 difference versus Dense-Only is small and
  retrieval-only; added gate-feature diagnostics remain too weak to support a
  strong end-to-end claim.
- No downstream Pass@1 or task-success result artifacts exist; the Pass@1
  schema file is documentation only.
- No human-labeled query-quality audit artifacts exist; the human-audit manifest
  template is documentation only.

These blockers are recorded in `SUBMISSION_READINESS.json`.
